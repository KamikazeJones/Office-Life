shadow$provide.module$node_modules$tone$build$esm$source$Noise = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Noise:{enumerable:!0, get:function() {
    return Noise;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioBuffer_DOT_js = require("module$node_modules$tone$build$esm$core$context$ToneAudioBuffer"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug"), require$_DOT__DOT__SLASH_source_SLASH_Source_DOT_js = require("module$node_modules$tone$build$esm$source$Source"), 
  require$_DOT__SLASH_buffer_SLASH_ToneBufferSource_DOT_js = require("module$node_modules$tone$build$esm$source$buffer$ToneBufferSource");
  class Noise extends require$_DOT__DOT__SLASH_source_SLASH_Source_DOT_js.Source {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(Noise.getDefaults(), arguments, ["type"]);
      super(options);
      this.name = "Noise";
      this._source = null;
      this._playbackRate = options.playbackRate;
      this.type = options.type;
      this._fadeIn = options.fadeIn;
      this._fadeOut = options.fadeOut;
    }
    static getDefaults() {
      return Object.assign(require$_DOT__DOT__SLASH_source_SLASH_Source_DOT_js.Source.getDefaults(), {fadeIn:0, fadeOut:0, playbackRate:1, type:"white"});
    }
    get type() {
      return this._type;
    }
    set type(type) {
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(type in _noiseBuffers, "Noise: invalid type: " + type);
      this._type !== type && (this._type = type, this.state === "started" && (type = this.now(), this._stop(type), this._start(type)));
    }
    get playbackRate() {
      return this._playbackRate;
    }
    set playbackRate(rate) {
      this._playbackRate = rate;
      this._source && (this._source.playbackRate.value = rate);
    }
    _start(time) {
      const buffer = _noiseBuffers[this._type];
      this._source = (new require$_DOT__SLASH_buffer_SLASH_ToneBufferSource_DOT_js.ToneBufferSource({url:buffer, context:this.context, fadeIn:this._fadeIn, fadeOut:this._fadeOut, loop:!0, onended:() => this.onstop(this), playbackRate:this._playbackRate})).connect(this.output);
      this._source.start(this.toSeconds(time), Math.random() * (buffer.duration - 0.001));
    }
    _stop(time) {
      this._source && (this._source.stop(this.toSeconds(time)), this._source = null);
    }
    get fadeIn() {
      return this._fadeIn;
    }
    set fadeIn(time) {
      this._fadeIn = time;
      this._source && (this._source.fadeIn = this._fadeIn);
    }
    get fadeOut() {
      return this._fadeOut;
    }
    set fadeOut(time) {
      this._fadeOut = time;
      this._source && (this._source.fadeOut = this._fadeOut);
    }
    _restart(time) {
      this._stop(time);
      this._start(time);
    }
    dispose() {
      super.dispose();
      this._source && this._source.disconnect();
      return this;
    }
  }
  var JSCompiler_object_inline_brown_179 = null, JSCompiler_object_inline_pink_180 = null, JSCompiler_object_inline_white_181 = null;
  const _noiseBuffers = {get brown() {
    if (!JSCompiler_object_inline_brown_179) {
      const buffer = [];
      for (let channelNum = 0; channelNum < 2; channelNum++) {
        const channel = new Float32Array(220500);
        buffer[channelNum] = channel;
        let lastOut = 0.0;
        for (let i = 0; i < 220500; i++) {
          channel[i] = (lastOut + 0.02 * (Math.random() * 2 - 1)) / 1.02, lastOut = channel[i], channel[i] *= 3.5;
        }
      }
      JSCompiler_object_inline_brown_179 = (new require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioBuffer_DOT_js.ToneAudioBuffer()).fromArray(buffer);
    }
    return JSCompiler_object_inline_brown_179;
  }, get pink() {
    if (!JSCompiler_object_inline_pink_180) {
      const buffer = [];
      for (let channelNum = 0; channelNum < 2; channelNum++) {
        const channel = new Float32Array(220500);
        buffer[channelNum] = channel;
        let b0, b1, b2, b3, b4, b5, b6;
        b0 = b1 = b2 = b3 = b4 = b5 = b6 = 0.0;
        for (let i = 0; i < 220500; i++) {
          const white = Math.random() * 2 - 1;
          b0 = 0.99886 * b0 + white * 0.0555179;
          b1 = 0.99332 * b1 + white * 0.0750759;
          b2 = 0.969 * b2 + white * 0.153852;
          b3 = 0.8665 * b3 + white * 0.3104856;
          b4 = 0.55 * b4 + white * 0.5329522;
          b5 = -0.7616 * b5 - white * 0.016898;
          channel[i] = b0 + b1 + b2 + b3 + b4 + b5 + b6 + white * 0.5362;
          channel[i] *= 0.11;
          b6 = white * 0.115926;
        }
      }
      JSCompiler_object_inline_pink_180 = (new require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioBuffer_DOT_js.ToneAudioBuffer()).fromArray(buffer);
    }
    return JSCompiler_object_inline_pink_180;
  }, get white() {
    if (!JSCompiler_object_inline_white_181) {
      const buffer = [];
      for (let channelNum = 0; channelNum < 2; channelNum++) {
        const channel = new Float32Array(220500);
        buffer[channelNum] = channel;
        for (let i = 0; i < 220500; i++) {
          channel[i] = Math.random() * 2 - 1;
        }
      }
      JSCompiler_object_inline_white_181 = (new require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioBuffer_DOT_js.ToneAudioBuffer()).fromArray(buffer);
    }
    return JSCompiler_object_inline_white_181;
  }};
};

//# sourceMappingURL=module$node_modules$tone$build$esm$source$Noise.js.map
