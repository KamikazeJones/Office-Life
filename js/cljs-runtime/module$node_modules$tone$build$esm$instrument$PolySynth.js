shadow$provide.module$node_modules$tone$build$esm$instrument$PolySynth = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, PolySynth:{enumerable:!0, get:function() {
    return PolySynth;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Midi_DOT_js = require("module$node_modules$tone$build$esm$core$type$Midi"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck"), require$_DOT__SLASH_Instrument_DOT_js = require("module$node_modules$tone$build$esm$instrument$Instrument"), 
  require$_DOT__SLASH_Monophonic_DOT_js = require("module$node_modules$tone$build$esm$instrument$Monophonic"), require$_DOT__SLASH_Synth_DOT_js = require("module$node_modules$tone$build$esm$instrument$Synth"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug");
  class PolySynth extends require$_DOT__SLASH_Instrument_DOT_js.Instrument {
    constructor() {
      var options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(PolySynth.getDefaults(), arguments, ["voice", "options"]);
      super(options);
      this.name = "PolySynth";
      this._availableVoices = [];
      this._activeVoices = [];
      this._voices = [];
      this._gcTimeout = -1;
      this._averageActiveVoices = 0;
      this._syncedRelease = time => this.releaseAll(time);
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(!(0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isNumber)(options.voice), "DEPRECATED: The polyphony count is no longer the first argument.");
      const defaults = options.voice.getDefaults();
      this.options = Object.assign(defaults, options.options);
      this.voice = options.voice;
      this.maxPolyphony = options.maxPolyphony;
      this._dummyVoice = this._getNextAvailableVoice();
      options = this._voices.indexOf(this._dummyVoice);
      this._voices.splice(options, 1);
      this._gcTimeout = this.context.setInterval(this._collectGarbage.bind(this), 1);
    }
    static getDefaults() {
      return Object.assign(require$_DOT__SLASH_Instrument_DOT_js.Instrument.getDefaults(), {maxPolyphony:32, options:{}, voice:require$_DOT__SLASH_Synth_DOT_js.Synth});
    }
    get activeVoices() {
      return this._activeVoices.length;
    }
    _makeVoiceAvailable(voice) {
      this._availableVoices.push(voice);
      const activeVoiceIndex = this._activeVoices.findIndex(e => e.voice === voice);
      this._activeVoices.splice(activeVoiceIndex, 1);
    }
    _getNextAvailableVoice() {
      if (this._availableVoices.length) {
        return this._availableVoices.shift();
      }
      if (this._voices.length < this.maxPolyphony) {
        const voice = new this.voice(Object.assign(this.options, {context:this.context, onsilence:this._makeVoiceAvailable.bind(this)}));
        (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(voice instanceof require$_DOT__SLASH_Monophonic_DOT_js.Monophonic, "Voice must extend Monophonic class");
        voice.connect(this.output);
        this._voices.push(voice);
        return voice;
      }
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.warn)("Max polyphony exceeded. Note dropped.");
    }
    _collectGarbage() {
      this._averageActiveVoices = Math.max(this._averageActiveVoices * 0.95, this.activeVoices);
      if (this._availableVoices.length && this._voices.length > Math.ceil(this._averageActiveVoices + 1)) {
        const firstAvail = this._availableVoices.shift(), index = this._voices.indexOf(firstAvail);
        this._voices.splice(index, 1);
        this.context.isOffline || firstAvail.dispose();
      }
    }
    _triggerAttack(notes, time, velocity) {
      notes.forEach(note => {
        const midiNote = (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Midi_DOT_js.MidiClass(this.context, note)).toMidi(), voice = this._getNextAvailableVoice();
        voice && (voice.triggerAttack(note, time, velocity), this._activeVoices.push({midi:midiNote, voice, released:!1}), this.log("triggerAttack", note, time));
      });
    }
    _triggerRelease(notes, time) {
      notes.forEach(note => {
        const midiNote = (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Midi_DOT_js.MidiClass(this.context, note)).toMidi(), event = this._activeVoices.find(({midi, released}) => midi === midiNote && !released);
        event && (event.voice.triggerRelease(time), event.released = !0, this.log("triggerRelease", note, time));
      });
    }
    _scheduleEvent(type, notes, time, velocity) {
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(!this.disposed, "Synth was already disposed");
      time <= this.now() ? type === "attack" ? this._triggerAttack(notes, time, velocity) : this._triggerRelease(notes, time) : this.context.setTimeout(() => {
        this.disposed || this._scheduleEvent(type, notes, time, velocity);
      }, time - this.now());
    }
    triggerAttack(notes, time, velocity) {
      Array.isArray(notes) || (notes = [notes]);
      time = this.toSeconds(time);
      this._scheduleEvent("attack", notes, time, velocity);
      return this;
    }
    triggerRelease(notes, time) {
      Array.isArray(notes) || (notes = [notes]);
      time = this.toSeconds(time);
      this._scheduleEvent("release", notes, time);
      return this;
    }
    triggerAttackRelease(notes, duration, time, velocity) {
      time = this.toSeconds(time);
      this.triggerAttack(notes, time, velocity);
      if ((0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(duration)) {
        for ((0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)((0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(notes), "If the duration is an array, the notes must also be an array"), velocity = 0; velocity < notes.length; velocity++) {
          const durationSeconds = this.toSeconds(duration[Math.min(velocity, duration.length - 1)]);
          (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(durationSeconds > 0, "The duration must be greater than 0");
          this.triggerRelease(notes[velocity], time + durationSeconds);
        }
      } else {
        duration = this.toSeconds(duration), (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(duration > 0, "The duration must be greater than 0"), this.triggerRelease(notes, time + duration);
      }
      return this;
    }
    sync() {
      this._syncState() && (this._syncMethod("triggerAttack", 1), this._syncMethod("triggerRelease", 1), this.context.transport.on("stop", this._syncedRelease), this.context.transport.on("pause", this._syncedRelease), this.context.transport.on("loopEnd", this._syncedRelease));
      return this;
    }
    set(options) {
      const sanitizedOptions = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.omitFromObject)(options, ["onsilence", "context"]);
      this.options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.deepMerge)(this.options, sanitizedOptions);
      this._voices.forEach(voice => voice.set(sanitizedOptions));
      this._dummyVoice.set(sanitizedOptions);
      return this;
    }
    get() {
      return this._dummyVoice.get();
    }
    releaseAll(time) {
      const computedTime = this.toSeconds(time);
      this._activeVoices.forEach(({voice}) => {
        voice.triggerRelease(computedTime);
      });
      return this;
    }
    dispose() {
      super.dispose();
      this._dummyVoice.dispose();
      this._voices.forEach(v => v.dispose());
      this._activeVoices = [];
      this._availableVoices = [];
      this.context.clearInterval(this._gcTimeout);
      return this;
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$instrument$PolySynth.js.map
