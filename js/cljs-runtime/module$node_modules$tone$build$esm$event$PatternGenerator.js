shadow$provide.module$node_modules$tone$build$esm$event$PatternGenerator = function(require, module, exports) {
  function* upPatternGen(numValues) {
    let index = 0;
    for (; index < numValues;) {
      index = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(index, 0, numValues - 1), yield index, index++;
    }
  }
  function* downPatternGen(numValues) {
    let index = numValues - 1;
    for (; index >= 0;) {
      index = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(index, 0, numValues - 1), yield index, index--;
    }
  }
  function* infiniteGen(numValues, gen) {
    for (;;) {
      yield* gen(numValues);
    }
  }
  function* alternatingGenerator(numValues, directionUp) {
    let index = directionUp ? 0 : numValues - 1;
    for (;;) {
      index = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(index, 0, numValues - 1), yield index, directionUp ? (index++, index >= numValues - 1 && (directionUp = !1)) : (index--, index <= 0 && (directionUp = !0));
    }
  }
  function* jumpUp(numValues) {
    let index = 0, stepIndex = 0;
    for (; index < numValues;) {
      index = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(index, 0, numValues - 1), yield index, stepIndex++, index += stepIndex % 2 ? 2 : -1;
    }
  }
  function* jumpDown(numValues) {
    let index = numValues - 1, stepIndex = 0;
    for (; index >= 0;) {
      index = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(index, 0, numValues - 1), yield index, stepIndex++, index += stepIndex % 2 ? -2 : 1;
    }
  }
  function* randomGen(numValues) {
    for (;;) {
      yield Math.floor(Math.random() * numValues);
    }
  }
  function* randomOnce(numValues) {
    const copy = [];
    for (var i = 0; i < numValues; i++) {
      copy.push(i);
    }
    for (; copy.length > 0;) {
      i = copy.splice(Math.floor(copy.length * Math.random()), 1), yield (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(i[0], 0, numValues - 1);
    }
  }
  function* randomWalk(numValues) {
    let index = Math.floor(Math.random() * numValues);
    for (;;) {
      index === 0 ? index++ : index === numValues - 1 ? index-- : Math.random() < 0.5 ? index-- : index++, yield index;
    }
  }
  function* PatternGenerator(numValues, pattern = "up", index) {
    (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(numValues >= 1, "The number of values must be at least one");
    switch(pattern) {
      case "up":
        yield* infiniteGen(numValues, upPatternGen);
      case "down":
        yield* infiniteGen(numValues, downPatternGen);
      case "upDown":
        yield* alternatingGenerator(numValues, !0);
      case "downUp":
        yield* alternatingGenerator(numValues, !1);
      case "alternateUp":
        yield* infiniteGen(numValues, jumpUp);
      case "alternateDown":
        yield* infiniteGen(numValues, jumpDown);
      case "random":
        yield* randomGen(numValues);
      case "randomOnce":
        yield* infiniteGen(numValues, randomOnce);
      case "randomWalk":
        yield* randomWalk(numValues);
    }
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, PatternGenerator:{enumerable:!0, get:function() {
    return PatternGenerator;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js = require("module$node_modules$tone$build$esm$core$util$Math");
};

//# sourceMappingURL=module$node_modules$tone$build$esm$event$PatternGenerator.js.map
