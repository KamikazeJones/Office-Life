shadow$provide.module$node_modules$tone$build$esm$core$util$Debug = function(require, module, exports) {
  function assert(statement, error) {
    if (!statement) {
      throw Error(error);
    }
  }
  function assertRange(value, gte, lte = Infinity) {
    if (!(gte <= value && value <= lte)) {
      throw new RangeError(`Value must be within [${gte}, ${lte}], got: ${value}`);
    }
  }
  function assertContextRunning(context) {
    context.isOffline || context.state === "running" || warn('The AudioContext is "suspended". Invoke Tone.start() from a user action to start the audio.');
  }
  function enterScheduledCallback(insideCallback) {
    isInsideScheduledCallback = insideCallback;
  }
  function assertUsedScheduleTime(time) {
    (0,require$_DOT__SLASH_TypeCheck_DOT_js.isUndef)(time) && isInsideScheduledCallback && !printedScheduledWarning && (printedScheduledWarning = !0, warn("Events scheduled inside of scheduled callbacks should use the passed in scheduling time. See https://github.com/Tonejs/Tone.js/wiki/Accurate-Timing"));
  }
  function setLogger(logger) {
    defaultLogger = logger;
  }
  function log(...args) {
    defaultLogger.log(...args);
  }
  function warn(...args) {
    defaultLogger.warn(...args);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, assert:{enumerable:!0, get:function() {
    return assert;
  }}, assertContextRunning:{enumerable:!0, get:function() {
    return assertContextRunning;
  }}, assertRange:{enumerable:!0, get:function() {
    return assertRange;
  }}, assertUsedScheduleTime:{enumerable:!0, get:function() {
    return assertUsedScheduleTime;
  }}, enterScheduledCallback:{enumerable:!0, get:function() {
    return enterScheduledCallback;
  }}, log:{enumerable:!0, get:function() {
    return log;
  }}, setLogger:{enumerable:!0, get:function() {
    return setLogger;
  }}, warn:{enumerable:!0, get:function() {
    return warn;
  }}});
  var require$_DOT__SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck");
  let isInsideScheduledCallback = !1, printedScheduledWarning = !1, defaultLogger = console;
};

//# sourceMappingURL=module$node_modules$tone$build$esm$core$util$Debug.js.map
