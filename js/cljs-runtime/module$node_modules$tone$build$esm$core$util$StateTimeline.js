shadow$provide.module$node_modules$tone$build$esm$core$util$StateTimeline = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, StateTimeline:{enumerable:!0, get:function() {
    return StateTimeline;
  }}});
  module = require("module$node_modules$tone$build$esm$core$util$Timeline");
  var require$_DOT__SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug");
  class StateTimeline extends module.Timeline {
    constructor(initial = "stopped") {
      super();
      this.name = "StateTimeline";
      this._initial = initial;
      this.setStateAtTime(this._initial, 0);
    }
    getValueAtTime(time) {
      time = this.get(time);
      return time !== null ? time.state : this._initial;
    }
    setStateAtTime(state, time, options) {
      (0,require$_DOT__SLASH_Debug_DOT_js.assertRange)(time, 0);
      this.add(Object.assign({}, options, {state, time}));
      return this;
    }
    getLastState(state, time) {
      for (time = this._search(time); time >= 0; time--) {
        const event = this._timeline[time];
        if (event.state === state) {
          return event;
        }
      }
    }
    getNextState(state, time) {
      time = this._search(time);
      if (time !== -1) {
        for (; time < this._timeline.length; time++) {
          const event = this._timeline[time];
          if (event.state === state) {
            return event;
          }
        }
      }
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$core$util$StateTimeline.js.map
