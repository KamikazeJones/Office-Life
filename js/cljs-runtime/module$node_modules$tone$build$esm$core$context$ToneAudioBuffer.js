shadow$provide.module$node_modules$tone$build$esm$core$context$ToneAudioBuffer = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, ToneAudioBuffer:{enumerable:!0, get:function() {
    return ToneAudioBuffer;
  }}});
  var require$tslib = require("module$node_modules$tslib$tslib"), require$_DOT__DOT__SLASH_Global_DOT_js = require("module$node_modules$tone$build$esm$core$Global");
  module = require("module$node_modules$tone$build$esm$core$Tone");
  var require$_DOT__DOT__SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface"), require$_DOT__DOT__SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck"), require$_DOT__DOT__SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug");
  class ToneAudioBuffer extends module.Tone {
    constructor() {
      super();
      this.name = "ToneAudioBuffer";
      this.onload = require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp;
      const options = (0,require$_DOT__DOT__SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(ToneAudioBuffer.getDefaults(), arguments, ["url", "onload", "onerror"]);
      this.reverse = options.reverse;
      this.onload = options.onload;
      (0,require$_DOT__DOT__SLASH_util_SLASH_TypeCheck_DOT_js.isString)(options.url) ? this.load(options.url).catch(options.onerror) : options.url && this.set(options.url);
    }
    static getDefaults() {
      return {onerror:require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp, onload:require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp, reverse:!1};
    }
    get sampleRate() {
      return this._buffer ? this._buffer.sampleRate : (0,require$_DOT__DOT__SLASH_Global_DOT_js.getContext)().sampleRate;
    }
    set(buffer) {
      buffer instanceof ToneAudioBuffer ? buffer.loaded ? this._buffer = buffer.get() : buffer.onload = () => {
        this.set(buffer);
        this.onload(this);
      } : this._buffer = buffer;
      this._reversed && this._reverse();
      return this;
    }
    get() {
      return this._buffer;
    }
    load(url) {
      return (0,require$tslib.__awaiter)(this, void 0, void 0, function*() {
        var doneLoading = ToneAudioBuffer.load(url).then(audioBuffer => {
          this.set(audioBuffer);
          this.onload(this);
        });
        ToneAudioBuffer.downloads.push(doneLoading);
        try {
          yield doneLoading;
        } finally {
          doneLoading = ToneAudioBuffer.downloads.indexOf(doneLoading), ToneAudioBuffer.downloads.splice(doneLoading, 1);
        }
        return this;
      });
    }
    dispose() {
      super.dispose();
      this._buffer = void 0;
      return this;
    }
    fromArray(array) {
      var isMultidimensional = (0,require$_DOT__DOT__SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(array) && array[0].length > 0;
      const channels = isMultidimensional ? array.length : 1;
      var len = isMultidimensional ? array[0].length : array.length;
      const context = (0,require$_DOT__DOT__SLASH_Global_DOT_js.getContext)();
      len = context.createBuffer(channels, len, context.sampleRate);
      array = isMultidimensional || channels !== 1 ? array : [array];
      for (isMultidimensional = 0; isMultidimensional < channels; isMultidimensional++) {
        len.copyToChannel(array[isMultidimensional], isMultidimensional);
      }
      this._buffer = len;
      return this;
    }
    toMono(chanNum) {
      if ((0,require$_DOT__DOT__SLASH_util_SLASH_TypeCheck_DOT_js.isNumber)(chanNum)) {
        this.fromArray(this.toArray(chanNum));
      } else {
        chanNum = new Float32Array(this.length);
        const numChannels = this.numberOfChannels;
        for (let channel = 0; channel < numChannels; channel++) {
          const channelArray = this.toArray(channel);
          for (let i = 0; i < channelArray.length; i++) {
            chanNum[i] += channelArray[i];
          }
        }
        chanNum = chanNum.map(sample => sample / numChannels);
        this.fromArray(chanNum);
      }
      return this;
    }
    toArray(channel) {
      if ((0,require$_DOT__DOT__SLASH_util_SLASH_TypeCheck_DOT_js.isNumber)(channel)) {
        return this.getChannelData(channel);
      }
      if (this.numberOfChannels === 1) {
        return this.toArray(0);
      }
      channel = [];
      for (let c = 0; c < this.numberOfChannels; c++) {
        channel[c] = this.getChannelData(c);
      }
      return channel;
    }
    getChannelData(channel) {
      return this._buffer ? this._buffer.getChannelData(channel) : new Float32Array(0);
    }
    slice(start, end = this.duration) {
      (0,require$_DOT__DOT__SLASH_util_SLASH_Debug_DOT_js.assert)(this.loaded, "Buffer is not loaded");
      start = Math.floor(start * this.sampleRate);
      end = Math.floor(end * this.sampleRate);
      (0,require$_DOT__DOT__SLASH_util_SLASH_Debug_DOT_js.assert)(start < end, "The start time must be less than the end time");
      var length = end - start;
      length = (0,require$_DOT__DOT__SLASH_Global_DOT_js.getContext)().createBuffer(this.numberOfChannels, length, this.sampleRate);
      for (let channel = 0; channel < this.numberOfChannels; channel++) {
        length.copyToChannel(this.getChannelData(channel).subarray(start, end), channel);
      }
      return new ToneAudioBuffer(length);
    }
    _reverse() {
      if (this.loaded) {
        for (let i = 0; i < this.numberOfChannels; i++) {
          this.getChannelData(i).reverse();
        }
      }
      return this;
    }
    get loaded() {
      return this.length > 0;
    }
    get duration() {
      return this._buffer ? this._buffer.duration : 0;
    }
    get length() {
      return this._buffer ? this._buffer.length : 0;
    }
    get numberOfChannels() {
      return this._buffer ? this._buffer.numberOfChannels : 0;
    }
    get reverse() {
      return this._reversed;
    }
    set reverse(rev) {
      this._reversed !== rev && (this._reversed = rev, this._reverse());
    }
    static fromArray(array) {
      return (new ToneAudioBuffer()).fromArray(array);
    }
    static fromUrl(url) {
      return (0,require$tslib.__awaiter)(this, void 0, void 0, function*() {
        return yield (new ToneAudioBuffer()).load(url);
      });
    }
    static load(url) {
      return (0,require$tslib.__awaiter)(this, void 0, void 0, function*() {
        var baseUrl = ToneAudioBuffer.baseUrl === "" || ToneAudioBuffer.baseUrl.endsWith("/") ? ToneAudioBuffer.baseUrl : ToneAudioBuffer.baseUrl + "/";
        baseUrl = yield fetch(baseUrl + url);
        if (!baseUrl.ok) {
          throw Error(`could not load url: ${url}`);
        }
        baseUrl = yield baseUrl.arrayBuffer();
        return yield (0,require$_DOT__DOT__SLASH_Global_DOT_js.getContext)().decodeAudioData(baseUrl);
      });
    }
    static supportsType(url) {
      url = url.split(".");
      url = url[url.length - 1];
      return document.createElement("audio").canPlayType("audio/" + url) !== "";
    }
    static loaded() {
      return (0,require$tslib.__awaiter)(this, void 0, void 0, function*() {
        for (yield Promise.resolve(); ToneAudioBuffer.downloads.length;) {
          yield ToneAudioBuffer.downloads[0];
        }
      });
    }
  }
  ToneAudioBuffer.baseUrl = "";
  ToneAudioBuffer.downloads = [];
};

//# sourceMappingURL=module$node_modules$tone$build$esm$core$context$ToneAudioBuffer.js.map
