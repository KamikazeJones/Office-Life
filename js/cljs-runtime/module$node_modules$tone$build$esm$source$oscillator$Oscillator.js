shadow$provide.module$node_modules$tone$build$esm$source$oscillator$Oscillator = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Oscillator:{enumerable:!0, get:function() {
    return Oscillator;
  }}});
  var require$tslib = require("module$node_modules$tslib$tslib"), require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface"), require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck"), 
  require$_DOT__DOT__SLASH__DOT__DOT__SLASH_signal_SLASH_Signal_DOT_js = require("module$node_modules$tone$build$esm$signal$Signal"), require$_DOT__DOT__SLASH_Source_DOT_js = require("module$node_modules$tone$build$esm$source$Source"), require$_DOT__SLASH_OscillatorInterface_DOT_js = require("module$node_modules$tone$build$esm$source$oscillator$OscillatorInterface"), require$_DOT__SLASH_ToneOscillatorNode_DOT_js = require("module$node_modules$tone$build$esm$source$oscillator$ToneOscillatorNode"), 
  require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug"), require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js = require("module$node_modules$tone$build$esm$core$util$Math");
  class Oscillator extends require$_DOT__DOT__SLASH_Source_DOT_js.Source {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(Oscillator.getDefaults(), arguments, ["frequency", "type"]);
      super(options);
      this.name = "Oscillator";
      this._oscillator = null;
      this.frequency = new require$_DOT__DOT__SLASH__DOT__DOT__SLASH_signal_SLASH_Signal_DOT_js.Signal({context:this.context, units:"frequency", value:options.frequency});
      (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.readOnly)(this, "frequency");
      this.detune = new require$_DOT__DOT__SLASH__DOT__DOT__SLASH_signal_SLASH_Signal_DOT_js.Signal({context:this.context, units:"cents", value:options.detune});
      (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.readOnly)(this, "detune");
      this._partials = options.partials;
      this._partialCount = options.partialCount;
      this._type = options.type;
      options.partialCount && options.type !== "custom" && (this._type = this.baseType + options.partialCount.toString());
      this.phase = options.phase;
    }
    static getDefaults() {
      return Object.assign(require$_DOT__DOT__SLASH_Source_DOT_js.Source.getDefaults(), {detune:0, frequency:440, partialCount:0, partials:[], phase:0, type:"sine"});
    }
    _start(time) {
      time = this.toSeconds(time);
      this._oscillator = new require$_DOT__SLASH_ToneOscillatorNode_DOT_js.ToneOscillatorNode({context:this.context, onended:() => this.onstop(this)});
      this._wave ? this._oscillator.setPeriodicWave(this._wave) : this._oscillator.type = this._type;
      this._oscillator.connect(this.output);
      this.frequency.connect(this._oscillator.frequency);
      this.detune.connect(this._oscillator.detune);
      this._oscillator.start(time);
    }
    _stop(time) {
      time = this.toSeconds(time);
      this._oscillator && this._oscillator.stop(time);
    }
    _restart(time) {
      time = this.toSeconds(time);
      this.log("restart", time);
      this._oscillator && this._oscillator.cancelStop();
      this._state.cancel(time);
      return this;
    }
    syncFrequency() {
      this.context.transport.syncSignal(this.frequency);
      return this;
    }
    unsyncFrequency() {
      this.context.transport.unsyncSignal(this.frequency);
      return this;
    }
    _getCachedPeriodicWave() {
      if (this._type === "custom") {
        return Oscillator._periodicWaveCache.find(description => description.phase === this._phase && (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.deepEquals)(description.partials, this._partials));
      }
      const oscProps = Oscillator._periodicWaveCache.find(description => description.type === this._type && description.phase === this._phase);
      this._partialCount = oscProps ? oscProps.partialCount : this._partialCount;
      return oscProps;
    }
    get type() {
      return this._type;
    }
    set type(type) {
      this._type = type;
      var isBasicType = ["sine", "square", "sawtooth", "triangle"].indexOf(type) !== -1;
      if (this._phase === 0 && isBasicType) {
        this._wave = void 0, this._partialCount = 0, this._oscillator !== null && (this._oscillator.type = type);
      } else {
        if (isBasicType = this._getCachedPeriodicWave(), (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isDefined)(isBasicType)) {
          const {partials, wave} = isBasicType;
          this._wave = wave;
          this._partials = partials;
          this._oscillator !== null && this._oscillator.setPeriodicWave(this._wave);
        } else {
          const [real, imag] = this._getRealImaginary(type, this._phase);
          this._wave = this.context.createPeriodicWave(real, imag);
          this._oscillator !== null && this._oscillator.setPeriodicWave(this._wave);
          Oscillator._periodicWaveCache.push({imag, partialCount:this._partialCount, partials:this._partials, phase:this._phase, real, type:this._type, wave:this._wave});
          Oscillator._periodicWaveCache.length > 100 && Oscillator._periodicWaveCache.shift();
        }
      }
    }
    get baseType() {
      return this._type.replace(this.partialCount.toString(), "");
    }
    set baseType(baseType) {
      this.type = this.partialCount && this._type !== "custom" && baseType !== "custom" ? baseType + this.partialCount : baseType;
    }
    get partialCount() {
      return this._partialCount;
    }
    set partialCount(p) {
      (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assertRange)(p, 0);
      let type = this._type;
      const partial = /^(sine|triangle|square|sawtooth)(\d+)$/.exec(this._type);
      partial && (type = partial[1]);
      if (this._type !== "custom") {
        this.type = p === 0 ? type : type + p.toString();
      } else {
        const fullPartials = new Float32Array(p);
        this._partials.forEach((v, i) => fullPartials[i] = v);
        this._partials = Array.from(fullPartials);
        this.type = this._type;
      }
    }
    _getRealImaginary(type, phase) {
      let periodicWaveSize = 2048;
      const real = new Float32Array(periodicWaveSize), imag = new Float32Array(periodicWaveSize);
      let partialCount = 1;
      if (type === "custom") {
        if (partialCount = this._partials.length + 1, this._partialCount = this._partials.length, periodicWaveSize = partialCount, this._partials.length === 0) {
          return [real, imag];
        }
      } else {
        var partial = /^(sine|triangle|square|sawtooth)(\d+)$/.exec(type);
        partial ? (partialCount = parseInt(partial[2], 10) + 1, this._partialCount = parseInt(partial[2], 10), type = partial[1], periodicWaveSize = partialCount = Math.max(partialCount, 2)) : this._partialCount = 0;
        this._partials = [];
      }
      for (partial = 1; partial < periodicWaveSize; ++partial) {
        var piFactor = 2 / (partial * Math.PI);
        switch(type) {
          case "sine":
            piFactor = partial <= partialCount ? 1 : 0;
            this._partials[partial - 1] = piFactor;
            break;
          case "square":
            piFactor = partial & 1 ? 2 * piFactor : 0;
            this._partials[partial - 1] = piFactor;
            break;
          case "sawtooth":
            piFactor *= partial & 1 ? 1 : -1;
            this._partials[partial - 1] = piFactor;
            break;
          case "triangle":
            piFactor = partial & 1 ? piFactor * piFactor * 2 * (partial - 1 >> 1 & 1 ? -1 : 1) : 0;
            this._partials[partial - 1] = piFactor;
            break;
          case "custom":
            piFactor = this._partials[partial - 1];
            break;
          default:
            throw new TypeError("Oscillator: invalid type: " + type);
        }
        piFactor !== 0 ? (real[partial] = -piFactor * Math.sin(phase * partial), imag[partial] = piFactor * Math.cos(phase * partial)) : (real[partial] = 0, imag[partial] = 0);
      }
      return [real, imag];
    }
    _inverseFFT(real, imag, phase) {
      let sum = 0;
      const len = real.length;
      for (let i = 0; i < len; i++) {
        sum += real[i] * Math.cos(i * phase) + imag[i] * Math.sin(i * phase);
      }
      return sum;
    }
    getInitialValue() {
      const [real, imag] = this._getRealImaginary(this._type, 0);
      let maxValue = 0;
      const twoPi = Math.PI * 2;
      for (let i = 0; i < 32; i++) {
        maxValue = Math.max(this._inverseFFT(real, imag, i / 32 * twoPi), maxValue);
      }
      return (0,require$_DOT__DOT__SLASH__DOT__DOT__SLASH_core_SLASH_util_SLASH_Math_DOT_js.clamp)(-this._inverseFFT(real, imag, this._phase) / maxValue, -1, 1);
    }
    get partials() {
      return this._partials.slice(0, this.partialCount);
    }
    set partials(partials) {
      this._partials = partials;
      this._partialCount = this._partials.length;
      partials.length && (this.type = "custom");
    }
    get phase() {
      return 180 / Math.PI * this._phase;
    }
    set phase(phase) {
      this._phase = phase * Math.PI / 180;
      this.type = this._type;
    }
    asArray() {
      return (0,require$tslib.__awaiter)(this, arguments, void 0, function*(length = 1024) {
        return (0,require$_DOT__SLASH_OscillatorInterface_DOT_js.generateWaveform)(this, length);
      });
    }
    dispose() {
      super.dispose();
      this._oscillator !== null && this._oscillator.dispose();
      this._wave = void 0;
      this.frequency.dispose();
      this.detune.dispose();
      return this;
    }
  }
  Oscillator._periodicWaveCache = [];
};

//# sourceMappingURL=module$node_modules$tone$build$esm$source$oscillator$Oscillator.js.map
