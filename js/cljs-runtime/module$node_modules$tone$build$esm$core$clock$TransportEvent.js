shadow$provide.module$node_modules$tone$build$esm$core$clock$TransportEvent = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, TransportEvent:{enumerable:!0, get:function() {
    return TransportEvent;
  }}});
  var require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface");
  class TransportEvent {
    constructor(transport, opts) {
      this.id = TransportEvent._eventId++;
      this._remainderTime = 0;
      opts = Object.assign(TransportEvent.getDefaults(), opts);
      this.transport = transport;
      this.callback = opts.callback;
      this._once = opts.once;
      this.time = Math.floor(opts.time);
      this._remainderTime = opts.time - this.time;
    }
    static getDefaults() {
      return {callback:require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp, once:!1, time:0};
    }
    get floatTime() {
      return this.time + this._remainderTime;
    }
    invoke(time) {
      if (this.callback) {
        const tickDuration = this.transport.bpm.getDurationOfTicks(1, time);
        this.callback(time + this._remainderTime * tickDuration);
        this._once && this.transport.clear(this.id);
      }
    }
    dispose() {
      this.callback = void 0;
      return this;
    }
  }
  TransportEvent._eventId = 0;
};

//# sourceMappingURL=module$node_modules$tone$build$esm$core$clock$TransportEvent.js.map
