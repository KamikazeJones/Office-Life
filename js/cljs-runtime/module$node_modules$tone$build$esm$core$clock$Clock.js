shadow$provide.module$node_modules$tone$build$esm$core$clock$Clock = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Clock:{enumerable:!0, get:function() {
    return Clock;
  }}});
  var require$_DOT__DOT__SLASH_context_SLASH_ToneWithContext_DOT_js = require("module$node_modules$tone$build$esm$core$context$ToneWithContext"), require$_DOT__DOT__SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults");
  module = require("module$node_modules$tone$build$esm$core$util$Emitter");
  var require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface"), require$_DOT__DOT__SLASH_util_SLASH_StateTimeline_DOT_js = require("module$node_modules$tone$build$esm$core$util$StateTimeline"), require$_DOT__SLASH_TickSource_DOT_js = require("module$node_modules$tone$build$esm$core$clock$TickSource"), require$_DOT__DOT__SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug");
  class Clock extends require$_DOT__DOT__SLASH_context_SLASH_ToneWithContext_DOT_js.ToneWithContext {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(Clock.getDefaults(), arguments, ["callback", "frequency"]);
      super(options);
      this.name = "Clock";
      this.callback = require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp;
      this._lastUpdate = 0;
      this._state = new require$_DOT__DOT__SLASH_util_SLASH_StateTimeline_DOT_js.StateTimeline("stopped");
      this._boundLoop = this._loop.bind(this);
      this.callback = options.callback;
      this._tickSource = new require$_DOT__SLASH_TickSource_DOT_js.TickSource({context:this.context, frequency:options.frequency, units:options.units});
      this._lastUpdate = 0;
      this.frequency = this._tickSource.frequency;
      (0,require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.readOnly)(this, "frequency");
      this._state.setStateAtTime("stopped", 0);
      this.context.on("tick", this._boundLoop);
    }
    static getDefaults() {
      return Object.assign(require$_DOT__DOT__SLASH_context_SLASH_ToneWithContext_DOT_js.ToneWithContext.getDefaults(), {callback:require$_DOT__DOT__SLASH_util_SLASH_Interface_DOT_js.noOp, frequency:1, units:"hertz"});
    }
    get state() {
      return this._state.getValueAtTime(this.now());
    }
    start(time, offset) {
      (0,require$_DOT__DOT__SLASH_util_SLASH_Debug_DOT_js.assertContextRunning)(this.context);
      time = this.toSeconds(time);
      this.log("start", time);
      this._state.getValueAtTime(time) !== "started" && (this._state.setStateAtTime("started", time), this._tickSource.start(time, offset), time < this._lastUpdate && this.emit("start", time, offset));
      return this;
    }
    stop(time) {
      time = this.toSeconds(time);
      this.log("stop", time);
      this._state.cancel(time);
      this._state.setStateAtTime("stopped", time);
      this._tickSource.stop(time);
      time < this._lastUpdate && this.emit("stop", time);
      return this;
    }
    pause(time) {
      time = this.toSeconds(time);
      this._state.getValueAtTime(time) === "started" && (this._state.setStateAtTime("paused", time), this._tickSource.pause(time), time < this._lastUpdate && this.emit("pause", time));
      return this;
    }
    get ticks() {
      return Math.ceil(this.getTicksAtTime(this.now()));
    }
    set ticks(t) {
      this._tickSource.ticks = t;
    }
    get seconds() {
      return this._tickSource.seconds;
    }
    set seconds(s) {
      this._tickSource.seconds = s;
    }
    getSecondsAtTime(time) {
      return this._tickSource.getSecondsAtTime(time);
    }
    setTicksAtTime(ticks, time) {
      this._tickSource.setTicksAtTime(ticks, time);
      return this;
    }
    getTimeOfTick(tick, before = this.now()) {
      return this._tickSource.getTimeOfTick(tick, before);
    }
    getTicksAtTime(time) {
      return this._tickSource.getTicksAtTime(time);
    }
    nextTickTime(offset, when) {
      when = this.toSeconds(when);
      const currentTick = this.getTicksAtTime(when);
      return this._tickSource.getTimeOfTick(currentTick + offset, when);
    }
    _loop() {
      const startTime = this._lastUpdate, endTime = this.now();
      this._lastUpdate = endTime;
      this.log("loop", startTime, endTime);
      startTime !== endTime && (this._state.forEachBetween(startTime, endTime, e => {
        switch(e.state) {
          case "started":
            const offset = this._tickSource.getTicksAtTime(e.time);
            this.emit("start", e.time, offset);
            break;
          case "stopped":
            e.time !== 0 && this.emit("stop", e.time);
            break;
          case "paused":
            this.emit("pause", e.time);
        }
      }), this._tickSource.forEachTickBetween(startTime, endTime, (time, ticks) => {
        this.callback(time, ticks);
      }));
    }
    getStateAtTime(time) {
      time = this.toSeconds(time);
      return this._state.getValueAtTime(time);
    }
    dispose() {
      super.dispose();
      this.context.off("tick", this._boundLoop);
      this._tickSource.dispose();
      this._state.dispose();
      return this;
    }
  }
  module.Emitter.mixin(Clock);
};

//# sourceMappingURL=module$node_modules$tone$build$esm$core$clock$Clock.js.map
