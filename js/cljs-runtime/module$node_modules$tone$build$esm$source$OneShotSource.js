shadow$provide.module$node_modules$tone$build$esm$source$OneShotSource = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, OneShotSource:{enumerable:!0, get:function() {
    return OneShotSource;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_Gain_DOT_js = require("module$node_modules$tone$build$esm$core$context$Gain"), require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioNode_DOT_js = require("module$node_modules$tone$build$esm$core$context$ToneAudioNode"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js = require("module$node_modules$tone$build$esm$core$util$Debug");
  class OneShotSource extends require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioNode_DOT_js.ToneAudioNode {
    constructor(options) {
      super(options);
      this.onended = require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp;
      this._timeout = this._stopTime = this._startTime = -1;
      this._gainNode = this.output = new require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_Gain_DOT_js.Gain({context:this.context, gain:0});
      this.getStateAtTime = function(time) {
        time = this.toSeconds(time);
        return this._startTime !== -1 && time >= this._startTime && (this._stopTime === -1 || time <= this._stopTime) ? "started" : "stopped";
      };
      this._fadeIn = options.fadeIn;
      this._fadeOut = options.fadeOut;
      this._curve = options.curve;
      this.onended = options.onended;
    }
    static getDefaults() {
      return Object.assign(require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneAudioNode_DOT_js.ToneAudioNode.getDefaults(), {curve:"linear", fadeIn:0, fadeOut:0, onended:require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp});
    }
    _startGain(time, gain = 1) {
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(this._startTime === -1, "Source cannot be started more than once");
      const fadeInTime = this.toSeconds(this._fadeIn);
      this._startTime = time + fadeInTime;
      this._startTime = Math.max(this._startTime, this.context.currentTime);
      fadeInTime > 0 ? (this._gainNode.gain.setValueAtTime(0, time), this._curve === "linear" ? this._gainNode.gain.linearRampToValueAtTime(gain, time + fadeInTime) : this._gainNode.gain.exponentialApproachValueAtTime(gain, time, fadeInTime)) : this._gainNode.gain.setValueAtTime(gain, time);
      return this;
    }
    stop(time) {
      this.log("stop", time);
      this._stopGain(this.toSeconds(time));
      return this;
    }
    _stopGain(time) {
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(this._startTime !== -1, "'start' must be called before 'stop'");
      this.cancelStop();
      const fadeOutTime = this.toSeconds(this._fadeOut);
      this._stopTime = this.toSeconds(time) + fadeOutTime;
      this._stopTime = Math.max(this._stopTime, this.now());
      fadeOutTime > 0 ? this._curve === "linear" ? this._gainNode.gain.linearRampTo(0, fadeOutTime, time) : this._gainNode.gain.targetRampTo(0, fadeOutTime, time) : (this._gainNode.gain.cancelAndHoldAtTime(time), this._gainNode.gain.setValueAtTime(0, time));
      this.context.clearTimeout(this._timeout);
      this._timeout = this.context.setTimeout(() => {
        const additionalTail = this._curve === "exponential" ? fadeOutTime * 2 : 0;
        this._stopSource(this.now() + additionalTail);
        this._onended();
      }, this._stopTime - this.context.currentTime);
      return this;
    }
    _onended() {
      if (this.onended !== require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp && (this.onended(this), this.onended = require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp, !this.context.isOffline)) {
        const disposeCallback = () => this.dispose();
        typeof requestIdleCallback !== "undefined" ? requestIdleCallback(disposeCallback) : setTimeout(disposeCallback, 10);
      }
    }
    get state() {
      return this.getStateAtTime(this.now());
    }
    cancelStop() {
      this.log("cancelStop");
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Debug_DOT_js.assert)(this._startTime !== -1, "Source is not started");
      this._gainNode.gain.cancelScheduledValues(this._startTime + this.sampleTime);
      this.context.clearTimeout(this._timeout);
      this._stopTime = -1;
      return this;
    }
    dispose() {
      super.dispose();
      this._gainNode.dispose();
      this.onended = require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp;
      return this;
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$source$OneShotSource.js.map
