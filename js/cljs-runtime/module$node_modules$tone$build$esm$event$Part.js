shadow$provide.module$node_modules$tone$build$esm$event$Part = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Part:{enumerable:!0, get:function() {
    return Part;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js = require("module$node_modules$tone$build$esm$core$type$Ticks"), require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_TransportTime_DOT_js = require("module$node_modules$tone$build$esm$core$type$TransportTime"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_StateTimeline_DOT_js = require("module$node_modules$tone$build$esm$core$util$StateTimeline"), 
  require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck"), require$_DOT__SLASH_ToneEvent_DOT_js = require("module$node_modules$tone$build$esm$event$ToneEvent");
  class Part extends require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(Part.getDefaults(), arguments, ["callback", "events"]);
      super(options);
      this.name = "Part";
      this._state = new require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_StateTimeline_DOT_js.StateTimeline("stopped");
      this._events = new Set();
      this._state.increasing = !0;
      options.events.forEach(event => {
        (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(event) ? this.add(event[0], event[1]) : this.add(event);
      });
    }
    static getDefaults() {
      return Object.assign(require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent.getDefaults(), {events:[]});
    }
    start(time, offset) {
      const ticks = this.toTicks(time);
      if (this._state.getValueAtTime(ticks) !== "started") {
        offset = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.defaultArg)(offset, this._loop ? this._loopStart : 0);
        offset = this._loop ? (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.defaultArg)(offset, this._loopStart) : (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.defaultArg)(offset, 0);
        const computedOffset = this.toTicks(offset);
        this._state.add({id:-1, offset:computedOffset, state:"started", time:ticks});
        this._forEach(event => {
          this._startNote(event, ticks, computedOffset);
        });
      }
      return this;
    }
    _startNote(event, ticks, offset) {
      ticks -= offset;
      this._loop ? event.startOffset >= this._loopStart && event.startOffset < this._loopEnd ? (event.startOffset < offset && (ticks += this._getLoopDuration()), event.start(new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, ticks))) : event.startOffset < this._loopStart && event.startOffset >= offset && (event.loop = !1, event.start(new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, ticks))) : event.startOffset >= offset && 
      event.start(new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, ticks));
    }
    get startOffset() {
      return this._startOffset;
    }
    set startOffset(offset) {
      this._startOffset = offset;
      this._forEach(event => {
        event.startOffset += this._startOffset;
      });
    }
    stop(time) {
      const ticks = this.toTicks(time);
      this._state.cancel(ticks);
      this._state.setStateAtTime("stopped", ticks);
      this._forEach(event => {
        event.stop(time);
      });
      return this;
    }
    at(time, value) {
      const timeInTicks = (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_TransportTime_DOT_js.TransportTimeClass(this.context, time)).toTicks(), tickTime = (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, 1)).toSeconds(), iterator = this._events.values();
      for (var result = iterator.next(); !result.done;) {
        result = result.value;
        if (Math.abs(timeInTicks - result.startOffset) < tickTime) {
          return (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isDefined)(value) && (result.value = value), result;
        }
        result = iterator.next();
      }
      return (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isDefined)(value) ? (this.add(time, value), this.at(time)) : null;
    }
    add(time, value) {
      time instanceof Object && Reflect.has(time, "time") && (value = time, time = value.time);
      time = this.toTicks(time);
      value instanceof require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent ? value.callback = this._tick.bind(this) : value = new require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent({callback:this._tick.bind(this), context:this.context, value});
      value.startOffset = time;
      value.set({humanize:this.humanize, loop:this.loop, loopEnd:this.loopEnd, loopStart:this.loopStart, playbackRate:this.playbackRate, probability:this.probability});
      this._events.add(value);
      this._restartEvent(value);
      return this;
    }
    _restartEvent(event) {
      this._state.forEach(stateEvent => {
        stateEvent.state === "started" ? this._startNote(event, stateEvent.time, stateEvent.offset) : event.stop(new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, stateEvent.time));
      });
    }
    remove(time, value) {
      (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isObject)(time) && time.hasOwnProperty("time") && (value = time, time = value.time);
      time = this.toTicks(time);
      this._events.forEach(event => {
        event.startOffset === time && ((0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isUndef)(value) || (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isDefined)(value) && event.value === value) && (this._events.delete(event), event.dispose());
      });
      return this;
    }
    clear() {
      this._forEach(event => event.dispose());
      this._events.clear();
      return this;
    }
    cancel(after) {
      this._forEach(event => event.cancel(after));
      this._state.cancel(this.toTicks(after));
      return this;
    }
    _forEach(callback) {
      this._events && this._events.forEach(event => {
        event instanceof Part ? event._forEach(callback) : callback(event);
      });
      return this;
    }
    _setAll(attr, value) {
      this._forEach(event => {
        event[attr] = value;
      });
    }
    _tick(time, value) {
      this.mute || this.callback(time, value);
    }
    _testLoopBoundries(event) {
      this._loop && (event.startOffset < this._loopStart || event.startOffset >= this._loopEnd) ? event.cancel(0) : event.state === "stopped" && this._restartEvent(event);
    }
    get probability() {
      return this._probability;
    }
    set probability(prob) {
      this._probability = prob;
      this._setAll("probability", prob);
    }
    get humanize() {
      return this._humanize;
    }
    set humanize(variation) {
      this._humanize = variation;
      this._setAll("humanize", variation);
    }
    get loop() {
      return this._loop;
    }
    set loop(loop) {
      this._loop = loop;
      this._forEach(event => {
        event.loopStart = this.loopStart;
        event.loopEnd = this.loopEnd;
        event.loop = loop;
        this._testLoopBoundries(event);
      });
    }
    get loopEnd() {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._loopEnd)).toSeconds();
    }
    set loopEnd(loopEnd) {
      this._loopEnd = this.toTicks(loopEnd);
      this._loop && this._forEach(event => {
        event.loopEnd = loopEnd;
        this._testLoopBoundries(event);
      });
    }
    get loopStart() {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._loopStart)).toSeconds();
    }
    set loopStart(loopStart) {
      this._loopStart = this.toTicks(loopStart);
      this._loop && this._forEach(event => {
        event.loopStart = this.loopStart;
        this._testLoopBoundries(event);
      });
    }
    get playbackRate() {
      return this._playbackRate;
    }
    set playbackRate(rate) {
      this._playbackRate = rate;
      this._setAll("playbackRate", rate);
    }
    get length() {
      return this._events.size;
    }
    dispose() {
      super.dispose();
      this.clear();
      return this;
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$event$Part.js.map
