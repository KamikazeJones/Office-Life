shadow$provide.module$node_modules$tone$build$esm$event$ToneEvent = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, ToneEvent:{enumerable:!0, get:function() {
    return ToneEvent;
  }}});
  require("module$node_modules$tone$build$esm$core$clock$Transport");
  var require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneWithContext_DOT_js = require("module$node_modules$tone$build$esm$core$context$ToneWithContext"), require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js = require("module$node_modules$tone$build$esm$core$type$Ticks"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js = require("module$node_modules$tone$build$esm$core$util$Interface"), 
  require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_StateTimeline_DOT_js = require("module$node_modules$tone$build$esm$core$util$StateTimeline"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck");
  class ToneEvent extends require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneWithContext_DOT_js.ToneWithContext {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(ToneEvent.getDefaults(), arguments, ["callback", "value"]);
      super(options);
      this.name = "ToneEvent";
      this._state = new require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_StateTimeline_DOT_js.StateTimeline("stopped");
      this._startOffset = 0;
      this._loop = options.loop;
      this.callback = options.callback;
      this.value = options.value;
      this._loopStart = this.toTicks(options.loopStart);
      this._loopEnd = this.toTicks(options.loopEnd);
      this._playbackRate = options.playbackRate;
      this._probability = options.probability;
      this._humanize = options.humanize;
      this.mute = options.mute;
      this._playbackRate = options.playbackRate;
      this._state.increasing = !0;
      this._rescheduleEvents();
    }
    static getDefaults() {
      return Object.assign(require$_DOT__DOT__SLASH_core_SLASH_context_SLASH_ToneWithContext_DOT_js.ToneWithContext.getDefaults(), {callback:require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Interface_DOT_js.noOp, humanize:!1, loop:!1, loopEnd:"1m", loopStart:0, mute:!1, playbackRate:1, probability:1, value:null});
    }
    _rescheduleEvents(after = -1) {
      this._state.forEachFrom(after, event => {
        let duration;
        if (event.state === "started") {
          event.id !== -1 && this.context.transport.clear(event.id);
          const startTick = event.time + Math.round(this.startOffset / this._playbackRate);
          if (this._loop === !0 || (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isNumber)(this._loop) && this._loop > 1) {
            duration = Infinity;
            (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isNumber)(this._loop) && (duration = this._loop * this._getLoopDuration());
            var nextEvent = this._state.getAfter(startTick);
            nextEvent !== null && (duration = Math.min(duration, nextEvent.time - startTick));
            duration !== Infinity && (duration = new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, duration));
            nextEvent = new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._getLoopDuration());
            event.id = this.context.transport.scheduleRepeat(this._tick.bind(this), nextEvent, new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, startTick), duration);
          } else {
            event.id = this.context.transport.schedule(this._tick.bind(this), new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, startTick));
          }
        }
      });
    }
    get state() {
      return this._state.getValueAtTime(this.context.transport.ticks);
    }
    get startOffset() {
      return this._startOffset;
    }
    set startOffset(offset) {
      this._startOffset = offset;
    }
    get probability() {
      return this._probability;
    }
    set probability(prob) {
      this._probability = prob;
    }
    get humanize() {
      return this._humanize;
    }
    set humanize(variation) {
      this._humanize = variation;
    }
    start(time) {
      time = this.toTicks(time);
      this._state.getValueAtTime(time) === "stopped" && (this._state.add({id:-1, state:"started", time}), this._rescheduleEvents(time));
      return this;
    }
    stop(time) {
      this.cancel(time);
      var ticks = this.toTicks(time);
      this._state.getValueAtTime(ticks) === "started" && (this._state.setStateAtTime("stopped", ticks, {id:-1}), time = this._state.getBefore(ticks), time !== null && (ticks = time.time), this._rescheduleEvents(ticks));
      return this;
    }
    cancel(time) {
      time = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.defaultArg)(time, -Infinity);
      time = this.toTicks(time);
      this._state.forEachFrom(time, event => {
        this.context.transport.clear(event.id);
      });
      this._state.cancel(time);
      return this;
    }
    _tick(time) {
      var ticks = this.context.transport.getTicksAtTime(time);
      this.mute || this._state.getValueAtTime(ticks) !== "started" || this.probability < 1 && Math.random() > this.probability || (this.humanize && (ticks = 0.02, (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isBoolean)(this.humanize) || (ticks = this.toSeconds(this.humanize)), time += (Math.random() * 2 - 1) * ticks), this.callback(time, this.value));
    }
    _getLoopDuration() {
      return (this._loopEnd - this._loopStart) / this._playbackRate;
    }
    get loop() {
      return this._loop;
    }
    set loop(loop) {
      this._loop = loop;
      this._rescheduleEvents();
    }
    get playbackRate() {
      return this._playbackRate;
    }
    set playbackRate(rate) {
      this._playbackRate = rate;
      this._rescheduleEvents();
    }
    get loopEnd() {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._loopEnd)).toSeconds();
    }
    set loopEnd(loopEnd) {
      this._loopEnd = this.toTicks(loopEnd);
      this._loop && this._rescheduleEvents();
    }
    get loopStart() {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._loopStart)).toSeconds();
    }
    set loopStart(loopStart) {
      this._loopStart = this.toTicks(loopStart);
      this._loop && this._rescheduleEvents();
    }
    get progress() {
      if (this._loop) {
        const ticks = this.context.transport.ticks, lastEvent = this._state.get(ticks);
        if (lastEvent !== null && lastEvent.state === "started") {
          const loopDuration = this._getLoopDuration();
          return (ticks - lastEvent.time) % loopDuration / loopDuration;
        }
      }
      return 0;
    }
    dispose() {
      super.dispose();
      this.cancel();
      this._state.dispose();
      return this;
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$event$ToneEvent.js.map
