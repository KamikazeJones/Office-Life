shadow$provide.module$node_modules$tone$build$esm$event$Sequence = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Sequence:{enumerable:!0, get:function() {
    return Sequence;
  }}});
  var require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js = require("module$node_modules$tone$build$esm$core$type$Ticks"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js = require("module$node_modules$tone$build$esm$core$util$Defaults"), require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js = require("module$node_modules$tone$build$esm$core$util$TypeCheck"), require$_DOT__SLASH_Part_DOT_js = require("module$node_modules$tone$build$esm$event$Part"), require$_DOT__SLASH_ToneEvent_DOT_js = 
  require("module$node_modules$tone$build$esm$event$ToneEvent");
  class Sequence extends require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent {
    constructor() {
      const options = (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.optionsFromArguments)(Sequence.getDefaults(), arguments, ["callback", "events", "subdivision"]);
      super(options);
      this.name = "Sequence";
      this._part = new require$_DOT__SLASH_Part_DOT_js.Part({callback:this._seqCallback.bind(this), context:this.context});
      this._events = [];
      this._eventsArray = [];
      this._subdivision = this.toTicks(options.subdivision);
      this.events = options.events;
      this.loop = options.loop;
      this.loopStart = options.loopStart;
      this.loopEnd = options.loopEnd;
      this.playbackRate = options.playbackRate;
      this.probability = options.probability;
      this.humanize = options.humanize;
      this.mute = options.mute;
      this.playbackRate = options.playbackRate;
    }
    static getDefaults() {
      return Object.assign((0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_Defaults_DOT_js.omitFromObject)(require$_DOT__SLASH_ToneEvent_DOT_js.ToneEvent.getDefaults(), ["value"]), {events:[], loop:!0, loopEnd:0, loopStart:0, subdivision:"8n"});
    }
    _seqCallback(time, value) {
      value === null || this.mute || this.callback(time, value);
    }
    get events() {
      return this._events;
    }
    set events(s) {
      this.clear();
      this._eventsArray = s;
      this._events = this._createSequence(this._eventsArray);
      this._eventsUpdated();
    }
    start(time, offset) {
      this._part.start(time, offset ? this._indexTime(offset) : offset);
      return this;
    }
    stop(time) {
      this._part.stop(time);
      return this;
    }
    get subdivision() {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, this._subdivision)).toSeconds();
    }
    _createSequence(array) {
      return new Proxy(array, {get:(target, property) => target[property], set:(target, property, value) => {
        (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isString)(property) && isFinite(parseInt(property, 10)) ? (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(value) ? target[property] = this._createSequence(value) : target[property] = value : target[property] = value;
        this._eventsUpdated();
        return !0;
      }});
    }
    _eventsUpdated() {
      this._part.clear();
      this._rescheduleSequence(this._eventsArray, this._subdivision, this.startOffset);
      this.loopEnd = this.loopEnd;
    }
    _rescheduleSequence(sequence, subdivision, startOffset) {
      sequence.forEach((value, index) => {
        index = index * subdivision + startOffset;
        (0,require$_DOT__DOT__SLASH_core_SLASH_util_SLASH_TypeCheck_DOT_js.isArray)(value) ? this._rescheduleSequence(value, subdivision / value.length, index) : (index = (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, index, "i")).toSeconds(), this._part.add(index, value));
      });
    }
    _indexTime(index) {
      return (new require$_DOT__DOT__SLASH_core_SLASH_type_SLASH_Ticks_DOT_js.TicksClass(this.context, index * this._subdivision + this.startOffset)).toSeconds();
    }
    clear() {
      this._part.clear();
      return this;
    }
    dispose() {
      super.dispose();
      this._part.dispose();
      return this;
    }
    get loop() {
      return this._part.loop;
    }
    set loop(l) {
      this._part.loop = l;
    }
    get loopStart() {
      return this._loopStart;
    }
    set loopStart(index) {
      this._loopStart = index;
      this._part.loopStart = this._indexTime(index);
    }
    get loopEnd() {
      return this._loopEnd;
    }
    set loopEnd(index) {
      this._loopEnd = index;
      this._part.loopEnd = index === 0 ? this._indexTime(this._eventsArray.length) : this._indexTime(index);
    }
    get startOffset() {
      return this._part.startOffset;
    }
    set startOffset(start) {
      this._part.startOffset = start;
    }
    get playbackRate() {
      return this._part.playbackRate;
    }
    set playbackRate(rate) {
      this._part.playbackRate = rate;
    }
    get probability() {
      return this._part.probability;
    }
    set probability(prob) {
      this._part.probability = prob;
    }
    get progress() {
      return this._part.progress;
    }
    get humanize() {
      return this._part.humanize;
    }
    set humanize(variation) {
      this._part.humanize = variation;
    }
    get length() {
      return this._part.length;
    }
  }
};

//# sourceMappingURL=module$node_modules$tone$build$esm$event$Sequence.js.map
